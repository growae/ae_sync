# aesync environment variables

# Database connection (PostgreSQL)
DATABASE_URL=postgresql://localhost:5432/{{PROJECT_NAME}}

# Or use PGlite for local development (no Postgres needed)
# DATABASE_KIND=pglite
# DATABASE_DIR=.aesync/pglite

# Server port
PORT=4200
